## EN
```
- Added an "AI content warning" message below the commit message textarea in English, Japanese, and Vietnamese locales.
- This warns users that AI-generated content may not be fully accurate and should be verified before committing.
- Also updated the `react-resizable-panels` dependency to `v2.1.9` and made minor UI adjustments to the `DataTable` and `MainPage` layout for improved consistency.
```

---

## JA
```
- コミットメッセージのテキストエリア下に、英語・日本語・ベトナム語対応の「AIコンテンツ警告」メッセージを追加しました。
- これにより、AIによって生成された内容が完全に正確でない可能性があることをユーザーに警告し、コミット前に内容を確認するよう促します。
- また、`react-resizable-panels` を `v2.1.9` にアップデートし、`DataTable` および `MainPage` レイアウトに小規模なUI調整を行い、一貫性を向上させました。
```

---

## VI
```
- Đã thêm thông báo "Cảnh báo nội dung AI" bên dưới ô nhập commit message với các ngôn ngữ: tiếng Anh, tiếng Nhật và tiếng Việt.
- Thông báo này cảnh báo rằng nội dung do AI tạo ra có thể không hoàn toàn chính xác và cần được kiểm tra kỹ lưỡng trước khi thực hiện commit.
- Ngoài ra, đã cập nhật thư viện `react-resizable-panels` lên phiên bản `v2.1.9` và thực hiện một số điều chỉnh nhỏ về giao diện của `DataTable` và bố cục `MainPage` để cải thiện - tính nhất quán.
```
