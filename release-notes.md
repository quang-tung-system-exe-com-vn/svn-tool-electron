## ✨ feat: Implement guided tour feature and enhance UI components

### 🇺🇸 English
```
- Added a guided tour feature using `react-joyride` to help users navigate the application.
- Introduced a welcome dialog that prompts users to start the tour on their first visit.
- Updated the TitleBar component to include a tour icon that triggers the tour.
- Enhanced the AboutDialog with improved styling and dark mode adjustment for the app logo.
- Modified the NewRevisionDialog to allow text wrapping in the commit message textarea.
- Adjusted the StatisticDialog layout for better visual consistency.
- Updated the Switch component styles for improved accessibility.
- Imported **Noto Color Emoji** font for better emoji rendering across platforms.
- Updated `global.css` font stack to include Noto Color Emoji.
- Adjusted Content Security Policy (CSP) in `index.html` to allow data URI images.
- Changed the position of the Toaster notification to top-center.
- Expanded translation files to support the tour feature in English, Japanese, and Vietnamese.
```
---

### 🇯🇵 Japanese
```
- アプリのナビゲーションを支援するため、`react-joyride` を使用したガイドツアー機能を追加しました。
- 初回起動時にツアー開始を促すウェルカムダイアログを導入しました。
- TitleBar コンポーネントにツアー開始アイコンを追加しました。
- AboutDialog をスタイル調整し、ダークモードにおけるアプリロゴの表示を改善しました。
- NewRevisionDialog のコミットメッセージ入力欄で改行・折り返しを可能にしました。
- StatisticDialog のレイアウトを調整し、見た目の一貫性を向上させました。
- Switch コンポーネントのアクセシビリティを改善しました。
- 絵文字の表示品質向上のため、**Noto Color Emoji** フォントを導入しました。
- `global.css` にてフォントスタックを更新し、Noto Color Emoji を追加しました。
- `index.html` の CSP を調整し、`data:` URI 画像の読み込みを許可しました。
- Toaster 通知の表示位置を画面上部中央に変更しました。
- 英語・日本語・ベトナム語でのツアー翻訳を追加しました。
```
---

### 🇻🇳 Vietnamese
```
- Thêm tính năng hướng dẫn người dùng bằng `react-joyride` để dễ dàng khám phá ứng dụng.
- Hiển thị hộp thoại chào mừng khi truy cập lần đầu để gợi ý bắt đầu tour.
- Cập nhật component TitleBar để có thêm nút bắt đầu tour.
- Cải thiện giao diện AboutDialog, điều chỉnh hiển thị logo trong chế độ tối.
- Cho phép xuống dòng trong textarea của NewRevisionDialog.
- Căn chỉnh lại bố cục StatisticDialog để nhất quán hơn.
- Nâng cấp style của Switch để dễ truy cập hơn.
- Import font **Noto Color Emoji** để emoji hiển thị đẹp và đúng trên nhiều hệ điều hành.
- Cập nhật `global.css` để thêm Noto Color Emoji vào danh sách font.
- Điều chỉnh CSP trong `index.html` để cho phép hiển thị ảnh từ `data:` URI.
- Di chuyển Toaster lên giữa phía trên để dễ thấy hơn.
- Thêm bản dịch tính năng tour cho tiếng Anh, tiếng Nhật và tiếng Việt.
```
